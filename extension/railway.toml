[build]
builder = "NIXPACKS"

[build.nixpacksPlan]
providers = ["node"]

[deploy]
startCommand = ""
# This service is not deployed; it is packaged into a browser extension.
# Run: pnpm --filter @zog/extension build
