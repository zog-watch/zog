[build]
builder = "NIXPACKS"

[build.nixpacksPlan]
providers = ["node"]

[deploy]
startCommand = ""
# This service is not deployed; it is built as a package consumed by web.
# Use `pnpm --filter @zog/providers build` in CI or as a dependency step.
